{
  "name": "sportdata-backend",
  "version": "3.0.0",
  "description": "SportData Backend — Node.js nativo + SQLite (sin frameworks)",
  "main": "server.js",
  "scripts": {
    "start": "node --experimental-sqlite server.js",
    "dev":   "node --experimental-sqlite --watch server.js"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "axios":       "^1.7.2",
    "bcryptjs":    "^3.0.3",
    "jsonwebtoken":"^9.0.3"
  }
}
