/**
 * SportData — Configuración de entorno
 * 
 * En Netlify: esta variable se inyecta via _redirects + env vars
 * Cambia RENDER_URL por la URL real de tu backend en Render
 * Ejemplo: https://sportdata-api.onrender.com
 */
window.SPORTDATA_API_URL = 'REPLACE_WITH_RENDER_URL';
